package com.liug.white.service.impl;

/**
 * Created by liugang on 2017/4/13.
 */
//@Service("userService")
public class UserServiceImpl {//implements UserService {

    /*@Autowired
    private UserRepository userRepository;

    public User loginValid(String userOrEmail, String pwd){
        User u = new User();
        u.setEmail(userOrEmail);
        u.setUserName(userOrEmail);
        User u_db = userRepository.getUserByNameOrEmail(userOrEmail);
        if(u_db!=null){
            if(u_db.getPassword().trim().equals(pwd.trim())) {
                u=u_db;
                u.setPassword(null);
                u.setLogin(true);
                u.setValid(true);
            }
        }
        return u;
    }*/

}
