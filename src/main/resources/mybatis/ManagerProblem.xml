<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.liug.dao.ManagerProblemMapper">
    <resultMap id="baseResultMap" type="com.liug.model.entity.ManagerProblem">


        <result column="id" property="id" jdbcType="BIGINT"/>
        <result column="summary" property="summary" jdbcType="VARCHAR"/>
        <result column="date" property="date" jdbcType="TIMESTAMP"/>
        <result column="channel" property="channel" jdbcType="VARCHAR"/>
        <result column="informer" property="informer" jdbcType="VARCHAR"/>
        <result column="finishdate" property="finishdate" jdbcType="TIMESTAMP"/>
        <result column="status" property="status" jdbcType="TINYINT"/>

        <result column="detail" property="detail" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="level" property="level" jdbcType="VARCHAR"/>
        <result column="system" property="system" jdbcType="VARCHAR"/>

        <result column="needtime" property="needtime" jdbcType="VARCHAR"/>
        <result column="needresources" property="needresources" jdbcType="VARCHAR"/>
        <result column="result" property="result" jdbcType="VARCHAR"/>
        <result column="dealer" property="dealer" jdbcType="VARCHAR"/>
        <result column="recorder" property="recorder" jdbcType="VARCHAR"/>

    </resultMap>
    <sql id="sql_column">
        id,
        summary,
        date,
        channel,
        informer,
        finishdate,
        status,

        detail,
        type,
        level,
        system,

        needtime,
        needresources,
        result,
        dealer,
        recorder
    </sql>

    <sql id="sql_column_where">
        <if test="id !=null ">
            AND id = #{id}
        </if>
        <if test="summary !=null and summary.length() != 0">
            AND summary = #{summary}
        </if>
        <if test="date !=null ">
            AND date = #{date}
        </if>

        <if test="channel !=null and channel.length() != 0">
            AND channel = #{channel}
        </if>
        <if test="informer !=null and informer.length() != 0">
            AND informer = #{informer}
        </if>
        <if test="finishdate !=null ">
            AND finishdate = #{finishdate}
        </if>
        <if test="status !=null ">
            AND status = #{status}
        </if>

        <if test="detail !=null and detail.length() != 0">
            AND detail = #{detail}
        </if>
        <if test="type !=null and type.length() != 0">
            AND type = #{type}
        </if>
        <if test="level !=null and level.length() != 0">
            AND level = #{level}
        </if>
        <if test="system !=null and system.length() != 0">
            AND system = #{system}
        </if>

        <if test="needtime !=null and needtime.length() != 0">
            AND needtime = #{needtime}
        </if>
        <if test="needresources !=null and needresources.length() != 0">
            AND needresources = #{needresources}
        </if>
        <if test="result !=null and result.length() != 0">
            AND result = #{result}
        </if>
        <if test="dealer !=null and dealer.length() != 0">
            AND dealer = #{dealer}
        </if>
        <if test="recorder !=null and recorder.length() != 0">
            AND recorder = #{recorder}
        </if>
    </sql>

    <insert id="insert" parameterType="com.liug.model.entity.ManagerProblem" useGeneratedKeys="true" keyProperty="id">
        insert into manager_problem
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="summary !=null and summary.length() != 0">
                summary,
            </if>
            <if test="date !=null ">
                date,
            </if>

            <if test="channel !=null and channel.length() != 0">
                channel,
            </if>
            <if test="informer !=null and informer.length() != 0">
                informer,
            </if>
            <if test="finishdate !=null ">
                finishdate,
            </if>
            <if test="status !=null ">
                status,
            </if>

            <if test="detail !=null and detail.length() != 0">
                detail,
            </if>
            <if test="type !=null and type.length() != 0">
                type,
            </if>
            <if test="level !=null and level.length() != 0">
                level,
            </if>
            <if test="system !=null and system.length() != 0">
                system,
            </if>

            <if test="needtime !=null and needtime.length() != 0">
                needtime,
            </if>
            <if test="needresources !=null and needresources.length() != 0">
                needresources,
            </if>
            <if test="result !=null and result.length() != 0">
                result,
            </if>
            <if test="dealer !=null and dealer.length() != 0">
                dealer,
            </if>
            <if test="recorder !=null and recorder.length() != 0">
                recorder,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="summary !=null and summary.length() != 0">
                #{summary},
            </if>
            <if test="date !=null ">
                #{date},
            </if>

            <if test="channel !=null and channel.length() != 0">
                #{channel},
            </if>
            <if test="informer !=null and informer.length() != 0">
                #{informer},
            </if>
            <if test="finishdate !=null ">
                #{finishdate},
            </if>
            <if test="status !=null ">
                #{status},
            </if>

            <if test="detail !=null and detail.length() != 0">
                #{detail},
            </if>
            <if test="type !=null and type.length() != 0">
                #{type},
            </if>
            <if test="level !=null and level.length() != 0">
                #{level},
            </if>
            <if test="system !=null and system.length() != 0">
                #{system},
            </if>

            <if test="needtime !=null and needtime.length() != 0">
                #{needtime},
            </if>
            <if test="needresources !=null and needresources.length() != 0">
                #{needresources},
            </if>
            <if test="result !=null and result.length() != 0">
                #{result},
            </if>
            <if test="dealer !=null and dealer.length() != 0">
                #{dealer},
            </if>
            <if test="recorder !=null and recorder.length() != 0">
                #{recorder},
            </if>
        </trim>
    </insert>


    <update id="update" parameterType="com.liug.model.entity.ManagerProblem">
        update manager_problem
        <set>
            <if test="summary !=null and summary.length() != 0">
                summary = #{summary},
            </if>
            <if test="date !=null ">
                date = #{date},
            </if>

            <if test="channel !=null and channel.length() != 0">
                channel = #{channel},
            </if>
            <if test="informer !=null and informer.length() != 0">
                informer = #{informer},
            </if>
            <if test="finishdate !=null ">
                finishdate = #{finishdate},
            </if>
            <if test="status !=null ">
                status = #{status},
            </if>

            <if test="detail !=null and detail.length() != 0">
                detail = #{detail},
            </if>
            <if test="type !=null and type.length() != 0">
                type = #{type},
            </if>
            <if test="level !=null and level.length() != 0">
                level = #{level},
            </if>
            <if test="system !=null and system.length() != 0">
                system = #{system},
            </if>

            <if test="needtime !=null and needtime.length() != 0">
                needtime = #{needtime},
            </if>
            <if test="needresources !=null and needresources.length() != 0">
                needresources = #{needresources},
            </if>
            <if test="result !=null and result.length() != 0">
                result = #{result},
            </if>
            <if test="dealer !=null and dealer.length() != 0">
                dealer = #{dealer},
            </if>
            <if test="recorder !=null and recorder.length() != 0">
                recorder = #{recorder},
            </if>
        </set>
        where id =#{id} and status !=7
    </update>
    <update id="deleteById">
        UPDATE manager_problem
        SET status = 7
        WHERE id = #{id}
    </update>

    <select id="select" parameterType="com.liug.model.entity.ManagerProblem" resultType="com.liug.model.entity.ManagerProblem">
        select * from manager_problem where
        <where>
            <include refid="sql_column_where"/>
        </where>
    </select>

    <select id="selectById" parameterType="long" resultType="com.liug.model.entity.ManagerProblem">
        SELECT *
        FROM manager_problem
        WHERE id = #{id} AND status != 7
    </select>


    <select id="selectAll"    resultType="com.liug.model.entity.ManagerProblem">
        SELECT *
        FROM manager_problem
        WHERE status != 7
        <if test="begin != null">
            AND date >= #{begin}
        </if>
        <if test="end != null">
            AND  #{end} >= date
        </if>
        <if test="sort!=''">
            ORDER BY ${sort}
        </if>
        <if test="order!=''">
            ${order}
        </if>
    </select>
    <select id="selectCounts" resultType="integer">
        SELECT count(*)
        FROM manager_problem
        WHERE status != 7
    </select>

</mapper>
