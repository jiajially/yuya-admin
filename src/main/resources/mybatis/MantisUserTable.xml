<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.liug.dao.MantisUserTableMapper">
    <resultMap id="baseResultMap" type="com.liug.model.entity.MantisUserTable">
        <result column="id" property="id" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="realname" property="realname" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="enabled" property="enabled" jdbcType="INTEGER"/>
    </resultMap>
    <sql id="sql_column">
id
username
realname
email
password
enabled
</sql>
    <sql id="sql_column_where">
        <if test="id !=null ">
            AND id = #{id}
        </if>
        <if test="username !=null and username.length() != 0">
            AND username = #{username}
        </if>
        <if test="realname !=null and realname.length() != 0">
            AND realname = #{realname}
        </if>
        <if test="email !=null and email.length() != 0">
            AND email = #{email}
        </if>
        <if test="password !=null and password.length() != 0">
            AND password = #{password}
        </if>
        <if test="enabled !=null ">
            AND enabled = #{enabled}
        </if>
    </sql>
    <insert id="insert" parameterType="com.liug.model.entity.MantisUserTable" useGeneratedKeys="true" keyProperty="id">
        insert into mantis_user_table
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username !=null and username.length() != 0">
                username,
            </if>
            <if test="realname !=null and realname.length() != 0">
                realname,
            </if>
            <if test="email !=null and email.length() != 0">
                email,
            </if>
            <if test="password !=null and password.length() != 0">
                password,
            </if>
            <if test="enabled !=null ">
                enabled,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="username !=null and username.length() != 0">
                #{username},
            </if>
            <if test="realname !=null and realname.length() != 0">
                #{realname},
            </if>
            <if test="email !=null and email.length() != 0">
                #{email},
            </if>
            <if test="password !=null and password.length() != 0">
                #{password},
            </if>
            <if test="enabled !=null ">
                #{enabled},
            </if>
        </trim>
    </insert>
    <update id="update" parameterType="com.liug.model.entity.MantisUserTable">
        update mantis_user_table
        <set>
            <if test="username !=null and username.length() != 0">
                username = #{username},
            </if>
            <if test="realname !=null and realname.length() != 0">
                realname = #{realname},
            </if>
            <if test="email !=null and email.length() != 0">
                email = #{email},
            </if>
            <if test="password !=null and password.length() != 0">
                password = #{password},
            </if>
            <if test="enabled !=null ">
                enabled = #{enabled},
            </if>
        </set>
        where id =#{id}
    </update>
    <update id="deleteById">
        UPDATE mantis_user_table
        SET enabled = 7
        WHERE id = #{id}
    </update>
    <select id="selectById" parameterType="long" resultType="com.liug.model.entity.MantisUserTable">
        SELECT *
        FROM mantis_user_table
        WHERE id = #{id}
    </select>
    <select id="selectByUsername" parameterType="String" resultType="com.liug.model.entity.MantisUserTable">
        SELECT *
        FROM mantis_user_table
        WHERE username = #{username}
    </select>
    <select id="selectAll" parameterType="long" resultType="com.liug.model.entity.MantisUserTable">
        SELECT *
        FROM mantis_user_table
    </select>
</mapper>